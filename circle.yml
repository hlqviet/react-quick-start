# circle.yml

test:
  override:
    - "true"

compile:
  override:
    - npm run build

deployment:
  push_to_server:
    branch: master
    commands:
      - rsync -avz build circleci@45.32.116.46:/usr/share/nginx/reactjs.blogcuaviet.com/html/
